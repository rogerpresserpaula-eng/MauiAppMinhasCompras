<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MauiAppMinhasCompras.Views.ListaProduto"
             Title="Minhas Compras">

    <ContentPage.ToolbarItems>

        <!-- NOVO -->
        <ToolbarItem Text="Relatório"
                     Clicked="ToolbarItem_Relatorio" />

        <ToolbarItem Text="Somar"
                     Clicked="ToolbarItem_Clicked_1" />

        <ToolbarItem Text="Adicionar"
                     Clicked="ToolbarItem_Clicked" />

    </ContentPage.ToolbarItems>

    <ContentPage.Resources>
        <ResourceDictionary>
            <Style TargetType="Label">
                <Setter Property="VerticalTextAlignment" Value="Center" />
            </Style>
        </ResourceDictionary>
    </ContentPage.Resources>

    <StackLayout>

        <SearchBar x:Name="txt_search" 
                   Margin="0, 0, 0, 0"
                   Placeholder="Busca de Produtos"
                   TextChanged="txt_search_TextChanged"/>

        <!-- NOVO -->
        <Picker x:Name="pk_categoria"
                Title="Filtrar por categoria"
                SelectedIndexChanged="pk_categoria_SelectedIndexChanged">

            <Picker.Items>
                <x:String>Todos</x:String>
                <x:String>Alimentos</x:String>
                <x:String>Higiene</x:String>
                <x:String>Limpeza</x:String>
                <x:String>Bebidas</x:String>
                <x:String>Outros</x:String>
            </Picker.Items>

        </Picker>

        <Frame Padding="0"
               Margin="10"
               VerticalOptions="FillAndExpand">

            <ListView x:Name="lst_produtos" 
                      IsPullToRefreshEnabled="True"
                      Refreshing="lst_produtos_Refreshing"
                      ItemSelected="lst_produtos_ItemSelected"
                      BackgroundColor="{StaticResource Gray200}">

                <ListView.Header>

                    <!-- ALTERADO: adicionado uma coluna -->
                    <Grid ColumnDefinitions="30, 100, 80, 80, 50, 90"
                          BackgroundColor="{StaticResource Gray500}">

                        <Label Grid.Column="0"
                               Text="ID"
                               Padding="5"
                               FontAttributes="Bold"/>

                        <Label Grid.Column="1"
                               Text="Descrição"
                               FontAttributes="Bold"/>

                        <!-- NOVO -->
                        <Label Grid.Column="2"
                               Text="Categoria"
                               FontAttributes="Bold"/>

                        <Label Grid.Column="3"
                               Text="Preço"
                               FontAttributes="Bold"/>

                        <Label Grid.Column="4"
                               Text="Qnt"
                               FontAttributes="Bold"/>

                        <Label Grid.Column="5"
                               Text="Total"
                               FontAttributes="Bold" />

                    </Grid>

                </ListView.Header>

                <ListView.ItemTemplate>

                    <DataTemplate>

                        <ViewCell>

                            <ViewCell.ContextActions>
                                <MenuItem Text="Remover"
                                          Clicked="MenuItem_Clicked" />
                            </ViewCell.ContextActions>

                            <!-- ALTERADO: adicionado uma coluna -->
                            <Grid ColumnDefinitions="30, 100, 80, 80, 50, 90">

                                <Label Grid.Column="0"
                                       Text="{Binding Id}"
                                       Padding="5" />

                                <Label Grid.Column="1"
                                       Text="{Binding Descricao}" />

                                <!-- NOVO -->
                                <Label Grid.Column="2"
                                       Text="{Binding Categoria}" />

                                <Label Grid.Column="3"
                                       Text="{Binding Preco, StringFormat='{}{0:c}'}" />

                                <Label Grid.Column="4"
                                       Text="{Binding Quantidade}" />

                                <Label Grid.Column="5"
                                       Text="{Binding Total, StringFormat='{}{0:c}'}" />

                            </Grid>

                        </ViewCell>

                    </DataTemplate>

                </ListView.ItemTemplate>

            </ListView>

        </Frame>

    </StackLayout>
</ContentPage>